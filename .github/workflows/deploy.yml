name: deploy

on:
  push:
    branches: [runner-test]

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - uses: actions/checkout@v4

      - name: build
        run: docker build -t api:latest .

      - name: stop old
        run: |
          docker stop api || true
          docker rm api || true

      - name: run new
        run: |
          docker run -d \
            --name api \
            -p 3000:3000 \
            --restart unless-stopped \
            api:latest